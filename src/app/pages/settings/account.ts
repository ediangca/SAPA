import { Component } from '@angular/core';

@Component({
    selector: 'settings-account',
    standalone: true,
    template: ` <div class="card">
        <div class="font-semibold text-xl mb-4">Account</div>
        <p>Update your account always!</p>
    </div>`
})
export class Account {}
